---
context_id: GUIDE_process_composer
version: "1.0.0"
type: guide
status: active
owner: ref101-specs
audience: developer
language: ru
last_updated: "2026-01-06"
related_documents:
  - path: ../WORKFLOW.md
    description: "Фазы и переходы workflow"
  - path: ../processes/README.md
    description: "Каталог реестра процессов"
  - path: ../templates/phases/RELEASE_TEMPLATE.md
    description: "Шаблон релизного контекста"
---

# Руководство по Process Composer

**Версия:** 1.0.0
**Последнее обновление:** 2026-01-06

> **Process Composer** позволяет создавать композиционные workflow из переиспользуемых фаз.

---

## Содержание

1. [Введение](#введение)
2. [Основные концепции](#основные-концепции)
3. [Структура определения процесса](#структура-определения-процесса)
4. [Создание пользовательских процессов](#создание-пользовательских-процессов)
5. [Порядок разрешения процессов](#порядок-разрешения-процессов)
6. [CLI-команды](#cli-команды)
7. [Примеры](#примеры)
8. [Устранение неполадок](#устранение-неполадок)

---

## Введение

### Что такое Process Composer?

**Process Composer** — это система композиции workflow, которая позволяет:

- **Составлять workflow** из переиспользуемых фаз
- **Создавать пользовательские процессы**, адаптированные под нужды проекта
- **Переопределять базовые процессы** проектно-специфичными вариантами
- **Валидировать определения процессов** по строгой схеме
- **Версионировать процессы** независимо от релизов

### Зачем нужен Process Composer?

**До Process Composer:**
```
WORKFLOW.md → Единый жёстко заданный 9-фазный процесс
              Нет гибкости для багфиксов, рефакторинга, документационных изменений
```

**После Process Composer:**
```
Библиотека процессов → feature_full (9 фаз)
                     → bugfix_simple (5 фаз)
                     → refactoring (6 фаз)
                     → custom_hotfix (ваш собственный процесс)
                     → ... (неограниченное количество процессов)
```

### Архитектура

```
┌─────────────────────────────────────────────────────────────┐
│  Определения процессов (JSON)                                │
│  processes/*.json                                            │
│  {project}/processes/*.json (переопределение)               │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│  Process Composer (tools/process-composer/)                  │
│  - ProcessRegistry: Загрузка с поддержкой переопределения   │
│  - ProcessValidator: Валидация по JSON Schema               │
│  - ProcessComposer: Композиция WorkflowDefinition           │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│  PCC (tools/command-center/)                                 │
│  - WorkflowEngine: Выполнение workflow                       │
│  - DataService: Отслеживание прогресса                       │
└─────────────────────────────────────────────────────────────┘
```

---

## Основные концепции

### 1. Определение процесса

**Процесс** — это JSON-файл, который определяет:
- **process_id** — Уникальный идентификатор (например, `feature_full`, `bugfix_simple`)
- **version** — Семантическая версия (например, `"1.0.0"`)
- **name** — Человекочитаемое название
- **description** — Для чего предназначен этот процесс
- **type** — Категория (например, `feature_development`, `bugfix`)
- **phases** — Массив определений фаз

### 2. Библиотека фаз

**Фазы** — это переиспользуемые шаги workflow, определённые в шаблонах:

```
templates/phases/
├── RELEASE_TEMPLATE.md          # Планирование релиза
├── BC_DELTA_TEMPLATE.md         # Бизнес-контекст
├── AC_DELTA_TEMPLATE.md         # Аналитический контекст
├── PLAN_FINALIZE_TEMPLATE.md    # Декомпозиция задач
├── PC_CONTEXT_TEMPLATE.md       # Реализация
├── IC_TEMPLATE.md               # Валидация инфраструктуры
├── QA_TESTING_TEMPLATE.md       # Тестирование
└── ... (будущие фазы)
```

Каждая фаза может использоваться в любом процессе.

### 3. Композиция

**Процесс = Комбинация фаз**

```json
{
  "process_id": "bugfix_simple",
  "phases": [
    { "id": "RELEASE" },
    { "id": "PC" },
    { "id": "IC" },
    { "id": "QA" },
    { "id": "DEPLOY" }
  ]
}
```

Это создаёт упрощённый workflow: **RELEASE → PC → IC → QA → DEPLOY** (пропускает BC/AC).

---

## Структура определения процесса

### JSON Schema

Все определения процессов валидируются по `processes/process.schema.json`:

```json
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "required": ["process_id", "version", "name", "phases"],
  "properties": {
    "process_id": {
      "type": "string",
      "pattern": "^[a-z_]+$",
      "description": "Только строчные буквы и подчёркивания"
    },
    "version": {
      "type": "string",
      "pattern": "^\\d+\\.\\d+\\.\\d+$",
      "description": "Семантическое версионирование (например, 1.0.0)"
    },
    "name": { "type": "string" },
    "description": { "type": "string" },
    "type": { "type": "string" },
    "phases": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["id"],
        "properties": {
          "id": { "type": "string" },
          "template": { "type": "string" },
          "validators": {
            "type": "array",
            "items": { "type": "string" }
          },
          "approval": {
            "type": "object",
            "properties": {
              "required": { "type": "boolean" },
              "role": { "type": "string" }
            }
          },
          "skip_allowed": { "type": "boolean" }
        }
      }
    }
  }
}
```

### Поля определения фазы

| Поле | Обязательное | Описание |
|------|--------------|----------|
| `id` | ✅ | Идентификатор фазы (например, `RELEASE`, `BC_delta`, `PC`) |
| `template` | ❌ | Имя файла шаблона (например, `RELEASE_TEMPLATE.md`) |
| `validators` | ❌ | Список ID валидаторов для запуска |
| `approval` | ❌ | Требования к согласованию |
| `approval.required` | ❌ | Требуется ли согласование |
| `approval.role` | ❌ | Кто согласует (например, `product_owner`, `tech_lead`) |
| `skip_allowed` | ❌ | Можно ли пропустить эту фазу |

---

## Создание пользовательских процессов

### Шаг 1: Выбрать ID процесса

Выберите уникальный идентификатор, используя **строчные буквы и подчёркивания**:

- ✅ `custom_hotfix`
- ✅ `emergency_deploy`
- ✅ `docs_only_update`
- ❌ `custom-hotfix` (дефисы не разрешены)
- ❌ `customHotfix` (camelCase не разрешён)

### Шаг 2: Создать JSON-файл

**Вариант A: Проектно-специфичный процесс** (рекомендуется для кастомизации)
```bash
mkdir -p processes
touch processes/custom_hotfix.json
```

**Вариант B: Глобальный процесс** (для широко используемых процессов)
```bash
touch processes/custom_hotfix.json
```

### Шаг 3: Определить структуру процесса

```json
{
  "process_id": "custom_hotfix",
  "version": "1.0.0",
  "name": "Экстренный хотфикс",
  "description": "Минимальный процесс для production-хотфиксов. Пропускает BC/AC/QA.",
  "type": "hotfix",
  "phases": [
    {
      "id": "RELEASE",
      "template": "RELEASE_TEMPLATE.md",
      "validators": ["release_has_problem"],
      "skip_allowed": false
    },
    {
      "id": "PC",
      "template": "PC_CONTEXT_TEMPLATE.md",
      "validators": ["pc_has_implementation"],
      "skip_allowed": false
    },
    {
      "id": "IC",
      "template": "IC_TEMPLATE.md",
      "validators": ["all_ics_validated"],
      "skip_allowed": false
    },
    {
      "id": "DEPLOY",
      "skip_allowed": false
    }
  ]
}
```

### Шаг 4: Валидировать процесс

```bash
pcc process-validate custom_hotfix
```

**Ожидаемый вывод:**
```
✅ Процесс custom_hotfix валиден
   - Определено 4 фазы
   - Требуется 0 согласований
   - 3 фазы с валидаторами
```

### Шаг 5: Использовать в релизе

В frontmatter вашего RELEASE-файла:

```yaml
---
context_id: RELEASE_v1_11_0_critical_fix
version: "1.11.0"
type: release
status: planning
process_id: custom_hotfix      # ← Используйте ваш процесс
process_version: "1.0.0"
---
```

---

## Порядок разрешения процессов

При загрузке процесса PCC ищет в следующем порядке:

```
1. {project}/processes/{process_id}.json      (высший приоритет)
2. processes/{process_id}.json                (запасной вариант)
3. Ошибка: Процесс не найден                  (если ни один не существует)
```

### Предупреждение о переопределении

Если существуют оба, PCC предупредит:

```
⚠️  Процесс bugfix_simple переопределён проектом
    (используется {project}/processes/bugfix_simple.json)
```

Это позволяет проектам кастомизировать базовые процессы, сохраняя стандарты ref101-specs.

---

## CLI-команды

### Список доступных процессов

```bash
pcc process-list
```

**Вывод:**
```
📋 Доступные процессы:

  [ref101]   feature_full        - Разработка фичи (Полный)
             [9 фаз, 4 согласования]
  [ref101]   bugfix_simple       - Багфикс (Простой)
             [5 фаз, 1 согласование]
  [project]  custom_hotfix       - Экстренный хотфикс
             [4 фазы, 0 согласований]
```

**Формат JSON:**
```bash
pcc process-list --format json
```

### Показать детали процесса

```bash
pcc process-show feature_full
```

**Вывод:**
```
📄 Процесс: Разработка фичи (Полный)
   ID: feature_full
   Версия: 1.0.0
   Тип: feature_development

   Полный процесс разработки фичи со всеми фазами.
   Используйте для новых фич с бизнес-контекстом, архитектурой и полной валидацией.

   Фазы (9):
     1.  RELEASE [шаблон: RELEASE_TEMPLATE.md]
         валидаторы: release_has_problem, release_has_scope, release_has_milestones
     2.  BC_delta [шаблон: BC_DELTA_TEMPLATE.md] [согласование: product_owner]
         валидаторы: bc_has_goals, bc_has_actors, bc_has_scenarios
     3.  AC_delta [шаблон: AC_DELTA_TEMPLATE.md] [согласование: tech_lead]
         валидаторы: ac_has_use_cases, ac_based_on_bc
     ...
```

**Формат JSON:**
```bash
pcc process-show feature_full --format json
```

### Валидировать определение процесса

```bash
pcc process-validate custom_hotfix
```

**Подробный вывод:**
```bash
pcc process-validate custom_hotfix --verbose
```

**Вывод:**
```
✅ Процесс custom_hotfix валиден
   - Определено 4 фазы
   - Требуется 0 согласований
   - 3 фазы с валидаторами
```

---

## Примеры

### Пример 1: Полная разработка фичи

**Файл:** `processes/feature_full.json`

```json
{
  "process_id": "feature_full",
  "version": "1.0.0",
  "name": "Разработка фичи (Полный)",
  "description": "Полный процесс разработки фичи со всеми фазами. Используйте для новых фич с бизнес-контекстом, архитектурой и полной валидацией.",
  "type": "feature_development",
  "phases": [
    {
      "id": "RELEASE",
      "template": "RELEASE_TEMPLATE.md",
      "validators": ["release_has_problem", "release_has_scope", "release_has_milestones"],
      "skip_allowed": false
    },
    {
      "id": "BC_delta",
      "template": "BC_DELTA_TEMPLATE.md",
      "validators": ["bc_has_goals", "bc_has_actors", "bc_has_scenarios"],
      "approval": {
        "required": true,
        "role": "product_owner"
      },
      "skip_allowed": false
    },
    {
      "id": "AC_delta",
      "template": "AC_DELTA_TEMPLATE.md",
      "validators": ["ac_has_use_cases", "ac_based_on_bc"],
      "approval": {
        "required": true,
        "role": "tech_lead"
      },
      "skip_allowed": false
    },
    {
      "id": "PLAN_FINALIZE",
      "template": "PLAN_FINALIZE_TEMPLATE.md",
      "validators": ["plan_has_tasks", "tasks_estimated"],
      "approval": {
        "required": true,
        "role": "team"
      },
      "skip_allowed": false
    },
    {
      "id": "PC",
      "template": "PC_CONTEXT_TEMPLATE.md",
      "validators": ["pc_has_implementation", "pc_has_5_ics"],
      "skip_allowed": false
    },
    {
      "id": "IC",
      "template": "IC_TEMPLATE.md",
      "validators": ["all_ics_validated", "no_p0_violations"],
      "skip_allowed": false
    },
    {
      "id": "QA",
      "template": "QA_TESTING_TEMPLATE.md",
      "validators": ["qa_tests_passed", "qa_coverage_met"],
      "approval": {
        "required": true,
        "role": "qa_lead"
      },
      "skip_allowed": false
    },
    {
      "id": "APPLY_DELTAS",
      "skip_allowed": false
    },
    {
      "id": "DEPLOY",
      "skip_allowed": false
    }
  ]
}
```

**Диаграмма workflow:**
```
RELEASE → BC_delta → AC_delta → PLAN_FINALIZE → PC → IC → QA → APPLY_DELTAS → DEPLOY
          (согласование) (согласование) (согласование)         (согласование)
```

**Сценарий использования:** Новые фичи с полным бизнес-анализом и валидацией.

---

### Пример 2: Простой багфикс

**Файл:** `processes/bugfix_simple.json`

```json
{
  "process_id": "bugfix_simple",
  "version": "1.0.0",
  "name": "Багфикс (Простой)",
  "description": "Упрощённый процесс для багфиксов. Пропускает BC/AC, сразу к PC → IC → QA → DEPLOY.",
  "type": "bugfix",
  "phases": [
    {
      "id": "RELEASE",
      "template": "RELEASE_TEMPLATE.md",
      "validators": ["release_has_problem"],
      "skip_allowed": false
    },
    {
      "id": "PC",
      "template": "PC_CONTEXT_TEMPLATE.md",
      "validators": ["pc_has_implementation", "pc_has_5_ics"],
      "skip_allowed": false
    },
    {
      "id": "IC",
      "template": "IC_TEMPLATE.md",
      "validators": ["all_ics_validated"],
      "skip_allowed": false
    },
    {
      "id": "QA",
      "template": "QA_TESTING_TEMPLATE.md",
      "validators": ["qa_tests_passed"],
      "approval": {
        "required": true,
        "role": "qa_lead"
      },
      "skip_allowed": false
    },
    {
      "id": "DEPLOY",
      "skip_allowed": false
    }
  ]
}
```

**Диаграмма workflow:**
```
RELEASE → PC → IC → QA → DEPLOY
                   (согласование)
```

**Сценарий использования:** Багфиксы, не требующие изменения бизнес-контекста.

---

### Пример 3: Пользовательский хотфикс (Проектно-специфичный)

**Файл:** `{project}/processes/custom_hotfix.json`

```json
{
  "process_id": "custom_hotfix",
  "version": "1.0.0",
  "name": "Экстренный хотфикс",
  "description": "Проектно-специфичный процесс хотфикса. Минимальная валидация для критических production-проблем.",
  "type": "hotfix",
  "phases": [
    {
      "id": "RELEASE",
      "template": "RELEASE_TEMPLATE.md",
      "validators": ["release_has_problem"],
      "skip_allowed": false
    },
    {
      "id": "PC",
      "template": "PC_CONTEXT_TEMPLATE.md",
      "validators": ["pc_has_implementation"],
      "skip_allowed": false
    },
    {
      "id": "IC",
      "template": "IC_TEMPLATE.md",
      "validators": ["all_ics_validated"],
      "skip_allowed": false
    },
    {
      "id": "DEPLOY",
      "skip_allowed": false
    }
  ]
}
```

**Диаграмма workflow:**
```
RELEASE → PC → IC → DEPLOY
```

**Сценарий использования:** Критические production-проблемы, требующие немедленного деплоя.

**⚠️ Предупреждение:** Этот процесс пропускает QA. Используйте только для экстренных случаев и обязательно проведите QA-тестирование позже.

---

## Устранение неполадок

### Ошибка: Процесс не найден

```
❌ Ошибка: Процесс не найден: my_custom_process
```

**Решение:**
1. Проверьте, что файл процесса существует:
   ```bash
   ls -la {project}/processes/my_custom_process.json
   ls -la processes/my_custom_process.json
   ```
2. Убедитесь, что `process_id` в JSON соответствует имени файла (без `.json`)

---

### Ошибка: Невалидное определение процесса

```
❌ Валидация процесса не прошла: data/process_id must match pattern "^[a-z_]+$"
```

**Решение:**
- Используйте только строчные буквы и подчёркивания в `process_id`
- Измените `my-custom-process` на `my_custom_process`

---

### Ошибка: Шаблон фазы не найден

```
❌ Валидация процесса не прошла: Шаблон фазы не найден: INVALID_TEMPLATE.md
```

**Решение:**
1. Проверьте, что шаблон существует:
   ```bash
   ls -la templates/phases/RELEASE_TEMPLATE.md
   ```
2. Используйте корректное имя файла шаблона из библиотеки фаз

---

### Предупреждение: Процесс переопределён проектом

```
⚠️  Процесс bugfix_simple переопределён проектом
    (используется {project}/processes/bugfix_simple.json)
```

**Это информационное сообщение, не ошибка.**

Если это непреднамеренно:
1. Удалите `{project}/processes/bugfix_simple.json`
2. PCC будет использовать версию ref101-specs

---

### Ошибка: Валидатор не найден

```
❌ Валидатор не найден: invalid_validator_id
```

**Решение:**
Используйте только валидаторы, определённые в реестре валидаторов PCC. Посмотрите доступные валидаторы:
```bash
pcc workflow --diagram  # Показывает все валидаторы
```

---

## Связанная документация

- [WORKFLOW.md](../WORKFLOW.md) — Фазы и переходы workflow
- [Реестр процессов](../processes/README.md) — Каталог всех процессов
- [RELEASE_TEMPLATE.md](../templates/phases/RELEASE_TEMPLATE.md) — Шаблон релизного контекста
- [PCC README](../../tools/command-center/README.md) — Документация инструмента PCC

---

## Changelog

### v1.0.0 — 2026-01-06

**Начальный релиз:**
- Документация архитектуры Process Composer
- Структура определения процесса (JSON Schema)
- Руководство по созданию пользовательских процессов
- Справочник CLI-команд
- Примеры (feature_full, bugfix_simple, custom_hotfix)
- Раздел устранения неполадок
