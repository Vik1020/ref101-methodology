# Методология каталогизации файлов и инструментов

**Version:** 1.0.0
**Last Updated:** 2026-01-04
**Status:** Active

---

## Общая философия

Проект ref101-specs использует **автогенерируемые каталоги** для управления файлами и инструментами.

**Принципы:**

1. **YAML как SSOT** - Единый источник истины в YAML формате (human-editable)
2. **Markdown как Documentation** - Auto-generated Markdown для чтения (human-readable)
3. **Git hooks** - Автоматические напоминания при добавлении новых файлов/инструментов
4. **Валидация** - Обязательная проверка консистентности перед коммитом

---

## 1. FILE_CATALOG - Каталог файлов проекта

### Назначение

Полный каталог всех значимых файлов проекта с классификацией по роли в проекте.

### Файлы

- **SSOT:** `catalogs/file-catalog.yaml`
- **Documentation:** `catalogs/FILE_CATALOG.md` (auto-generated)
- **Generator:** `tools/generate-file-catalog.ts`
- **Validator:** `tools/validate-file-catalog.ts`
- **Git hook:** `.claude/hooks/pre-commit-file-catalog.sh`

### Система классификации (7 категорий)

#### ENTRY_POINT - Точки входа

**Назначение:** Первые файлы, которые читает человек или LLM
**Примеры:** CLAUDE.md, README.md, INDEX.md
**Mutability:** immutable (редко меняются, стабильные)
**Audience:** both (human + llm)

#### SSOT - Single Source of Truth

**Назначение:** Канонические файлы с авторитетными данными
**Примеры:** WORKFLOW.md, SYSTEM_PROMPT.md, catalogs/file-catalog.yaml
**Mutability:** versioned (изменения через версионирование)
**Audience:** both

#### TEMPLATE - Шаблоны

**Назначение:** Blueprints для создания новых контекстов
**Примеры:** BC_DELTA_TEMPLATE.md, AC_DELTA_TEMPLATE.md, PC_CONTEXT_TEMPLATE.md
**Mutability:** versioned
**Audience:** llm (используются Claude для генерации)

#### GUIDE - Руководства

**Назначение:** How-To документы, checklists, процессы
**Примеры:** CODE_REVIEW_CHECKLIST.md, TESTING_STRATEGY.md, DEPLOYMENT_GUIDE.md
**Mutability:** versioned
**Audience:** both

#### STANDARD - Стандарты (Infrastructure Contexts)

**Назначение:** Обязательные требования (IC: security, a11y, performance, monitoring)
**Примеры:** ic_security_input_sanitization/ic.md, ic_a11y_standards/ic.md
**Mutability:** versioned
**Audience:** llm (соблюдаются в PC contexts)

#### TOOL - Инструменты

**Назначение:** Исполняемые скрипты, утилиты, CLI приложения
**Примеры:** generate-file-catalog.ts, validateContext.ts, command-center/
**Mutability:** versioned
**Audience:** llm (запускаются Claude или разработчиком)

#### WORKING_DOC - Рабочие документы

**Назначение:** Изменяемые файлы с текущим состоянием (BC/AC domains, deltas)
**Примеры:** docs/domains/BC_DOMAIN_*.md, docs/releases/v*/BC_delta_*.md, docs/releases/v*/RELEASE_*.md
**Mutability:** mutable (изменяются часто)
**Audience:** both

### Дополнительные атрибуты

#### mutability (изменяемость)

- **immutable** - редко меняется (README, CLAUDE.md)
- **versioned** - изменяется через версионирование (WORKFLOW.md, templates)
- **mutable** - изменяется часто (working docs, domains)

#### mastered (освоенность)

- **yes** - полностью освоен Claude
- **partial** - частично освоен
- **no** - не освоен (требует изучения)

#### audience (аудитория)

- **human** - для людей (текстовая документация)
- **llm** - для Claude (templates, tools, specifications)
- **both** - для обоих (README, INDEX, guides)

---

## 2. TOOLS_CATALOG - Каталог инструментов проекта

### Назначение

Полный каталог всех инструментов (scripts, CLI, hooks) с многомерной классификацией.

### Файлы

- **SSOT:** `catalogs/tools-catalog.yaml`
- **Documentation:** `catalogs/TOOLS_CATALOG.md` (auto-generated)
- **Generator:** `tools/generate-tools-catalog.ts`
- **Validator:** `tools/validate-tools-catalog.ts`
- **Git hook:** `.claude/hooks/pre-commit-tools-catalog.sh`

### Многомерная система классификации (7 измерений)

#### Измерение 1: PURPOSE (Назначение) - ОСНОВНОЕ

**6 категорий:**

1. **VALIDATION** - Проверка корректности
   - Возвращают pass/fail, блокируют при ошибках
   - Примеры: validateContext, validate-domain, checkCompliance

2. **GENERATION** - Генерация файлов/кода
   - Создают новые файлы по шаблонам
   - Примеры: generateContext, generate-file-catalog

3. **ANALYSIS** - Анализ и метрики
   - Собирают данные, строят графы, вычисляют метрики
   - Примеры: collectMetrics, traceability, context-graph

4. **INTEGRATION** - Интеграция с внешними системами
   - API клиенты, синхронизация, observability
   - Примеры: tracker_client, yandex-cloud-logger

5. **WORKFLOW** - Управление рабочим процессом
   - Оркестрация, CLI интерфейсы, dashboard
   - Примеры: pcc (command-center), apply-delta, rebuild-domain

6. **AUTOMATION** - Автоматизация (Git hooks)
   - Триггеры на события git/claude
   - Примеры: pre-commit-file-catalog, PostToolUse hooks

#### Измерение 2: LAYER (Уровень абстракции)

1. **infrastructure** - Низкоуровневые утилиты (парсеры, API клиенты)
2. **tool** - Инструменты среднего уровня (самостоятельные скрипты)
3. **application** - Высокоуровневые приложения (комплексные с множеством команд)

#### Измерение 3: USAGE_MODE (Способ использования)

Массив значений (можно несколько):

- **cli** - Можно запустить из командной строки напрямую
- **npm** - Доступен через npm script
- **api** - Используется как библиотека (import/require)
- **hook** - Git hook (автоматический триггер)
- **web** - Web UI (браузерный интерфейс)

#### Измерение 4: SCOPE (Область применения)

1. **project** - Специфичен для ref101-specs (знает о структуре проекта)
2. **domain** - Domain-specific (применим к методологии BC/AC/PC/IC)
3. **universal** - Универсальный (можно использовать в любом проекте)

#### Измерение 5: RUNTIME (Среда выполнения)

1. **node** - Node.js/TypeScript/JavaScript
2. **bash** - Shell scripts
3. **browser** - Web UI (для pcc dashboard)

#### Измерение 6: MATURITY (Зрелость) - опционально

1. **stable** - Стабильный, production-ready
2. **beta** - Бета-версия, основной функционал готов
3. **alpha** - Альфа-версия, в активной разработке
4. **prototype** - Прототип, экспериментальный

#### Измерение 7: IO_TYPE (Тип входа/выхода) - опционально

1. **file→file** - Читает файлы, пишет файлы
2. **file→api** - Читает файлы, обновляет API
3. **api→file** - Вызывает API, пишет файлы
4. **file→stdout** - Читает файлы, выводит в терминал
5. **interactive** - Интерактивный ввод-вывод

### Метрика adoption (Используемость в проекте)

**6 уровней:**

1. 🔴 **essential** - Критически важный (без него проект не работает)
2. 🟠 **required** - Обязательный (обязателен по спецификации/workflow)
3. 🟡 **recommended** - Рекомендованный (лучшие практики, но не критичен)
4. 🟢 **optional** - Опциональный (дополнительные возможности)
5. 🔵 **experimental** - Экспериментальный (в разработке, может измениться)
6. ⚫ **deprecated** - Устаревший (не рекомендуется использовать)

### Группировка multi-tool инструментов

**Проблема:** command-center содержит 13 команд разных категорий

**Решение:** Иерархическая структура с `parent_tool`

```yaml
# Родительский инструмент
- path: "tools/command-center/"
  category: WORKFLOW
  is_parent: true
  has_sub_tools: 13

# Дочерние инструменты
- path: "tools/command-center/src/cli/commands/status.ts"
  category: ANALYSIS  # Собственная категория команды!
  parent_tool: "tools/command-center/"
  command_name: "status"
  cli_usage: "pcc status"
```

**Преимущества:**

- Каждая команда в правильной категории (status → ANALYSIS, validate → VALIDATION)
- Связь с родительским приложением сохранена
- Можно фильтровать по parent_tool или category

---

## 3. Workflow: Как поддерживать актуальность

### При добавлении нового файла

1. Git hook `.claude/hooks/pre-commit-file-catalog.sh` выдаст предупреждение
2. Добавить запись в `catalogs/file-catalog.yaml`:
   ```yaml
   - path: "новый_файл.md"
     category: GUIDE
     description: "Описание файла"
     mutability: versioned
     mastered: no
     audience: both
   ```
3. Запустить генератор:
   ```bash
   npm run generate:file-catalog
   ```
4. Закоммитить оба файла (YAML + MD)

### При добавлении нового инструмента

1. Git hook `.claude/hooks/pre-commit-tools-catalog.sh` выдаст предупреждение
2. Добавить запись в `catalogs/tools-catalog.yaml`:
   ```yaml
   - path: "tools/new-tool.ts"
     category: VALIDATION
     layer: tool
     usage_modes: [cli, npm]
     scope: domain
     runtime: node
     adoption: optional
     mastered: no
     description: "Описание инструмента"
   ```
3. Запустить генератор:
   ```bash
   npm run generate:tools-catalog
   ```
4. Обновить `catalogs/file-catalog.yaml` (добавить запись о new-tool.ts)
5. Запустить:
   ```bash
   npm run generate:file-catalog
   ```
6. Закоммитить все файлы

---

## 4. Валидация

### catalogs/file-catalog.yaml

```bash
npm run validate:file-catalog
```

**Проверки:**

- ✅ Все пути существуют
- ✅ Обязательные поля заполнены
- ✅ Валидные enum значения (category, mutability, mastered, audience)
- ✅ Нет дубликатов

### catalogs/tools-catalog.yaml

```bash
npm run validate:tools-catalog
```

**Проверки:**

- ✅ Все пути существуют
- ✅ Обязательные поля заполнены
- ✅ Валидные enum значения (category, layer, scope, runtime, adoption, mastered, usage_modes)
- ✅ Нет дубликатов
- ✅ parent_tool ссылается на существующий инструмент
- ✅ is_parent=true имеет has_sub_tools > 0

---

## 5. Связь с другими системами

### catalogs/file-catalog.yaml и catalogs/tools-catalog.yaml

**Все инструменты также должны быть в catalogs/file-catalog.yaml!**

Пример:

```yaml
# В catalogs/tools-catalog.yaml
- path: "tools/generate-file-catalog.ts"
  category: GENERATION
  layer: tool
  adoption: required

# В catalogs/file-catalog.yaml (та же самая запись!)
- path: "tools/generate-file-catalog.ts"
  category: TOOL
  description: "Generator: catalogs/file-catalog.yaml → catalogs/FILE_CATALOG.md"
  mutability: versioned
  mastered: yes
  audience: llm
```

**Почему дублирование?**

- catalogs/file-catalog.yaml - "ЧТО это за файл" (роль в проекте)
- catalogs/tools-catalog.yaml - "КАК использовать инструмент" (назначение, способы запуска)

---

## 6. Changelog

### v1.0.0 - 2026-01-04

- Создан единый документ методологии для обоих каталогов
- Описаны 7 категорий FILE_CATALOG
- Описаны 7 измерений TOOLS_CATALOG
- Добавлены workflow и валидация
